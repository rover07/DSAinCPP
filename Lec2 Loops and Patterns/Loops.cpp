#include <iostream>
using namespace std;
int main()
{
    /* int n;
    cin >> n;
    int i = 1;
    while (i <= n)
    {
        cout << i;
        i++;
    } */

    /*  // Sum of 1 to N
     int n, i = 1, sum = 0;
     cout << "Enter value of n";
     cin >> n;
     while (i <= n)
     {
         sum = sum + i;
         i++;
     }
     cout << "The sum is=" << sum; */

    /*     // Homework: find sum of even numbers from 1 to N
        int n, i = 0, sum = 0;
        cout << "Enter number" << endl;
        cin >> n;
        while (i <= n)
        {
            sum = sum + i;
            i = i + 2;
        }
        cout << "The sum of even number upto" << n << "is" << sum;
     */
    // Homework:fahrenhite to celcius table

  /*   // prime or not
    int n;
    cin >> n;
    int i = 2; // 2 coz 1 is not prime
    while (i < n)
    {
        if (n % i == 0)
        {
            cout << "is not Prime for" << i << endl;
        }
        else
        {
            cout << "is prime for" << i << endl;
        }
        i = i + 1;
    } */


    
}